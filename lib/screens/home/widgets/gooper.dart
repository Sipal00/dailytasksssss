import 'package:flutter/material.dart';

class gopremium extends StatelessWidget {
  const gopremium({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("go permium"),
    );
  }
}
